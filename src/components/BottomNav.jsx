import React from'react';import{NavLink}from'react-router-dom';import Icon from'./Icon';const items=[['/','home','Home'],['/premium','crown','Premium'],['/downloads','download','Downloads'],['/me','user','Me']];export default function BottomNav(){return <nav className="bottom-nav">{items.map(([to,icon,label])=><NavLink end={to==='/' } key={to} to={to}>{<Icon name={icon} size={19}/>}<span>{label}</span></NavLink>)}</nav>}
