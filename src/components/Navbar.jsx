import React,{useState}from'react';import{Link,useNavigate}from'react-router-dom';import Icon from'./Icon';import{useApp}from'../context/AppContext';export default function Navbar(){const{language,setLanguage}=useApp();const[q,setQ]=useState('');const nav=useNavigate();const go=e=>{e.preventDefault();if(q.trim()){nav(`/search?q=${encodeURIComponent(q.trim())}`)}};return <header className="topbar"><Link className="brand" to="/"><b>ZXH</b><span>MOVIE APP</span></Link><nav className="desktop-nav"><Link to="/">Home</Link><Link to="/premium">Premium</Link><Link to="/downloads">Downloads</Link><Link to="/me">Me</Link></nav><form className="search" onSubmit={go}><Icon name="search" size={18}/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search movies, shows..." aria-label="Search"/><button type="submit" hidden/></form><div className="nav-actions"><button className="lang" onClick={()=>setLanguage(language==='en-US'?'en-US':'en-US')}>EN</button><Link className="avatar" to="/me">Z</Link></div></header>}
