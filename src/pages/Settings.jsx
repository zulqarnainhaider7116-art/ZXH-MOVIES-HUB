import React from'react';import{useApp}from'../context/AppContext';export default function Settings(){const{language,setLanguage}=useApp();return <div className="container page"><span className="eyebrow">PREFERENCES</span><h1>Settings</h1><div className="settings-card"><label>Metadata language<select value={language} onChange={e=>setLanguage(e.target.value)}><option value="en-US">English (US)</option><option value="en-GB">English (UK)</option></select></label><p>Playback sources and TMDB metadata remain separate. API keys are read only from environment variables.</p></div></div>}
